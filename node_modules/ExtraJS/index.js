global.getEnv = function($variable, $default = null){
	if(process.env === undefined) {
		console.warn('No ENV file found.');
		process.exit(1);
	}
	return (process.env[$variable] !== undefined) ? process.env[$variable] : $default;
}

require('dotenv').config({ path: require('path').dirname(require.main.filename) + '/.env' });
require('./connection.js');
require('./utils.js');

module.exports = {
	Model: require('./Models/Model.js'),
	Collection: require('./Models/Collection.js'),
	DB: require('./Models/DB.js'),
	Listener: require('./Models/Listener.js')
}